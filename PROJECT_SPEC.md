Read PROJECT_SPEC.md completely and treat it as the source of truth for this project.

This is a new project. Implement PHASE 1 ONLY.

Phase 1 scope:
- inspect the current folder first
- establish the production project architecture
- scaffold the application
- create the Dhira design system/tokens
- configure typography, colors, spacing and responsive foundations
- create the database schema and migrations
- create realistic demo seed data for the initial Dhira products
- create the basic storefront/admin application shells
- create .env.example and secure .gitignore
- establish shared types/services needed for later phases
- use the supplied Dhira logo from /reference without modifying it

Do NOT build checkout, Cashfree, Shiprocket, analytics, or all customer pages yet.
Do NOT rush into generating the entire website.
Do NOT hardcode product/business data into UI components.

Architecture must follow PROJECT_SPEC.md and remain production-grade and scalable.

Before implementing, briefly inspect the folder and make only necessary architectural decisions. Then implement Phase 1 directly in the files.

Run:
- install
- lint
- typecheck
- tests/build as applicable

Fix any errors you introduce before finishing.

TOKEN RULE:
Do not explain code file-by-file and do not repeat PROJECT_SPEC.md.
Spend tokens on implementation.

Final response maximum 8 concise lines containing only:
1. what was implemented
2. tests/build result
3. any genuine blocker
4. recommended next phase

Start now.